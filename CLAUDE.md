# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

This is a minimal static HTML page project with no build system, package manager, tests, or dependencies. The entire project consists of a single `index.html` file containing inline CSS.

## Development

There are no build, lint, or test commands — this is plain static HTML. To view changes, open `index.html` directly in a browser (e.g. `open index.html` on macOS).

## Structure

- `index.html` — the only source file; all styling is inline via a `<style>` block in the `<head>`.
